node(label: 'linux') {
  checkout scm
  sh 'docker build -t ipfs-companion .'
  sh 'docker run -i ipfs-companion yarn ci:test'
  sh 'docker run -i -v $(pwd)/build:/usr/src/app/build ipfs-companion yarn ci:build'
  archiveArtifacts artifacts: 'build/*.zip'
}
